# InterviewForge AI 学习教练分阶段实施计划

> 版本：v2.1
>
> 制定时间：2026-09-07
>
> 当前状态：阶段 3.1 已完成并交接，未部署，等待用户确认是否进入阶段 4。
>
> 重要约定：除非用户明确说“开始阶段 X”“施工”或提出具体实现要求，否则只讨论和维护计划，不修改业务代码、不安装依赖、不迁移数据库、不部署。

本文件是 AI 学习教练后续施工与交接的主计划。原有 `docs/AI学习教练与上下文工程规划.md` 保留为需求背景；实际施工顺序和完成标准以本文件为准。

---

## 1. 建设目标

在 InterviewForge 已有题目、书架、学习记录、间隔复习和多用户体系上增加一个 AI 学习教练，逐步实现：

- 一键分析学习情况；
- 识别反复 WA、AC 后又 WA、多次浏览但未 AC、长期逾期等问题；
- 分析书架模块进度和学习断点；
- 根据用户目标推荐书籍和学习顺序；
- 根据已有基础和可用时间生成学习计划；
- 支持围绕分析和路线继续追问；
- 用户确认后，将计划写入系统；
- AI 不可用时，原有学习功能仍然完整可用。

核心原则：

> 后端负责计算事实和筛选证据，LangChain 负责模型调用、结构化输出和后期工具编排，AI 负责解释和规划。

---

## 2. 简化后的施工与交接方式

不为每一步建立重复审计流程。每个阶段只保留四个必要环节：

```text
明确本阶段范围
  → 施工与自测
  → 一次集中审查并修复
  → 验收、交接，必要时部署
```

### 2.1 每阶段只交付一份报告

阶段完成后写一份交接报告：

```text
docs/ai-handoffs/phase-0.md
docs/ai-handoffs/phase-1.md
...
```

报告只记录真正有用的信息：

- 完成了什么；
- 修改了哪些文件；
- 数据库和配置是否变化；
- 测试结果；
- 集中审查发现和修复的问题；
- 已知限制；
- 是否部署、如何回滚；
- 下一阶段可以从哪里继续。

不另外创建重复的测试报告、审计报告和部署报告；证据统一放入该阶段交接报告。

### 2.2 审查强度按风险决定

| 改动类型 | 所需检查 |
|---|---|
| 文档、数据字典、Prompt 草案 | 自检 + 一次内容审阅 |
| 只读统计和上下文编译 | 自动测试 + 一次集中代码审查 |
| 模型调用、外部网络、数据库新表 | 自动测试 + 集中代码/安全审查 + 灰度验证 |
| 会写入用户计划的 Agent 工具 | 自动测试 + 权限与确认流程专项检查 |

不对低风险阶段套用生产级安全审计，但涉及用户数据、密钥、外部模型和写操作时不能省略必要检查。

### 2.3 审查次数与成本上限

为减少不必要的时间和 token 消耗，后续阶段固定采用以下规则：

- 默认只有“实施代理自测 + 一次独立集中审查”；
- 集中审查通过后立即交接，不再追加保险性复审；
- 只有发现 P0/P1 阻断问题时才安排修复，并且只复查对应问题，不重新进行全量审查；
- P2 优化项默认写入交接报告，不阻塞当前阶段，除非它会直接影响数据正确性、安全或现有功能；
- 同一组完整测试默认只由实施代理运行一次、主代理最终验收一次，不让多个代理重复执行相同测试；
- 一个文件集合同时只允许一个实施代理写入；审查代理只读；
- 子代理任务必须有明确目标、写入范围、完成条件和交接格式，避免开放式扫描；
- 主代理只负责阶段拆分、任务分配、阻断项判断和大目标验收，不重复实现已经委派的工作。

### 2.4 阶段开始规则

- 每个阶段必须由用户明确要求开始；
- 只施工当前指定阶段，不顺手实现后续阶段；
- 当前阶段审查交接完成后，等待用户决定是否进入下一阶段；
- 是否部署由用户指令和该阶段计划共同决定；
- 计划文档的修改不视为施工授权。

---

## 3. 技术路线：LangChain 渐进引入

### 3.1 结论

后续建议使用 Python LangChain，但不从第一阶段就引入。

采用以下边界：

| 层 | 推荐实现 | 原因 |
|---|---|---|
| 学习指标和 SQL 聚合 | 原生 Python + SQLite | 必须确定、可测试，不应依赖模型框架 |
| 上下文筛选和脱敏 | 原生 Python | 规则清晰，便于限制数据和做泄漏测试 |
| 模型适配与结构化输出 | LangChain | 统一模型接口，支持结构化响应和供应商切换 |
| 一键学习分析 | LangChain ChatModel + Pydantic 结构 | 单次受控调用，不需要完整 Agent |
| 课程推荐解释 | LangChain 结构化输出 | 候选课程先由规则召回，AI 只排序和解释 |
| 多轮工具调用 | LangChain `create_agent` | 适合白名单只读工具调用 |
| 持久化、暂停确认、恢复执行 | 必要时使用 LangGraph | 只在多轮 Agent 和人工确认复杂到需要状态图时加入 |

### 3.2 为什么这样使用 LangChain

LangChain 当前将模型、工具、Agent 和结构化输出作为核心能力，模型集成放在独立供应商包中。它适合本项目后续切换不同 OpenAI-compatible 模型，并用 Pydantic 约束 AI 输出。[安装说明](https://docs.langchain.com/oss/python/langchain/install)、[模型与结构化输出](https://docs.langchain.com/oss/python/langchain/models)

LangChain Agent 可以调用普通 Python 函数形式的工具，并支持结构化工具结果，适合后期封装 `get_learning_summary()`、`search_modules()` 等只读能力。[Agent 文档](https://docs.langchain.com/oss/python/langchain/agents)、[Tools 文档](https://docs.langchain.com/oss/python/langchain/tools)

LangGraph 提供检查点、持久化和人工确认能力，但第一版一键分析没有必要使用完整图工作流。只有进入多轮对话和写计划确认时再评估使用。[LangGraph 持久化](https://docs.langchain.com/oss/python/langgraph/persistence)、[Human-in-the-loop](https://docs.langchain.com/oss/python/langchain/human-in-the-loop)

### 3.3 依赖控制

当前后端主要使用 Python 标准库。引入 LangChain 时应保持 AI 为可选能力：

- 单独维护 `requirements-ai.txt`；
- 固定经过验证的版本，不在生产环境使用无限制升级；
- 只安装实际使用的核心包和一个模型供应商包；
- 使用延迟导入，未安装 AI 依赖时原站仍可启动；
- `AI_ENABLED=0` 时不加载模型、不发起外部请求；
- 不为了 LangChain 重写现有数据库、认证和 HTTP 服务。

第一版预计依赖：

```text
langchain
对应模型的 LangChain provider package
pydantic
```

具体包名和版本在实际进入模型接入阶段时，根据当时官方版本验证后锁定。

### 3.4 不使用 LangChain 的部分

以下逻辑始终由项目自己控制：

- 用户认证和权限；
- 用户数据库路径；
- 学习指标计算；
- 轮次和逾期规则；
- 敏感字段白名单；
- 上下文预算；
- 缓存归属；
- 写计划前的用户确认；
- 模型输出中的题号、课程 ID 和证据 ID 校验。

---

## 同类项目调研与借鉴决策（施工前依据）

本节记录 2026-09-07 的施工前调研。结论不是选择一个项目照搬，而是提取已经被不同系统验证过的设计模式，再按 InterviewForge 当前 Python、SQLite、多用户和 VPS 部署条件做减法。

### 调研对象与成熟度判断

| 项目 | 定位与可参考点 | 判断 |
|---|---|---|
| [OATutor](https://github.com/CAHLR/OATutor) | 开源自适应辅导系统；使用知识点映射、课程计划、分技能掌握度和 BKT | 研究基础和实现都较完整，适合借鉴“题目—技能—掌握度”数据结构；不照搬 React/Firebase 和完整 BKT |
| [Moodle Analytics](https://moodledev.io/docs/5.0/apis/subsystems/analytics) | 将指标、目标、洞察、动作和动作结果连接起来；静态规则模型也是正式模型 | 概念最成熟，适合定义本项目学习分析的责任边界和反馈闭环 |
| [Adaptive AI Tutor](https://github.com/UAnirudh/adaptive-ai-tutor) | 保存掌握度、错误、近期会话摘要和长期记忆，并对每类上下文设置 Top-N | 项目较新，只借鉴上下文裁剪和学生模型字段，不作为成熟依赖或算法权威 |
| [LearningMAP](https://github.com/ai-for-edu/LearningMAP) | 状态化学习流程、独立学生记忆、后台任务、结构化响应校验和页面状态恢复 | 架构思路可借鉴；现阶段不迁移 FastAPI，也不直接引入完整 LangGraph 和向量记忆 |
| [OpenTutor](https://github.com/zijinz456/OpenTutor) | 本地优先、自适应复习、知识图谱、功能成熟度标记和安全基线 | 适合借鉴可选 AI、功能状态标记和密钥检查；其单用户默认不适用于本站 |
| [LangChain Context Engineering](https://docs.langchain.com/oss/python/langchain/context-engineering) | 区分单次模型上下文、会话状态和跨会话长期记忆，支持裁剪、摘要、重试和限制 | 作为阶段 3 以后框架设计依据；先用静态 Prompt 和固定上下文，按需要逐项启用中间件 |

上述项目技术栈、用户规模和目标都不同。任何代码、数据或内容在采用前必须单独检查许可证；本计划只吸收架构模式，不复制第三方课程内容和生成产物。

### 各项目的具体可借鉴点

- **OATutor**：其问题内容、课程计划和技能模型彼此分离，题目可映射到多个知识点，掌握度也按知识点计算。InterviewForge 应借鉴这种中心化映射，但先用可解释规则和置信度，不直接套用需要校准 slip、guess、transit 等参数的 BKT。
- **Moodle Analytics**：其分析模型明确区分 indicators、target、insights 和 actions，并允许纯静态规则成为正式分析模型。InterviewForge 因此先做确定性指标，并记录用户对建议采取了什么动作，而不是一开始训练预测模型。
- **Adaptive AI Tutor**：其学生上下文将掌握度、未解决错误、近期会话摘要和长期记忆分开，并给数据库查询与最终 Prompt 分别设置条目上限。可借鉴双层限额、错误频次/最后出现时间/解决状态和“近期摘要代替全部历史”；不采用其未经本站数据验证的固定加权公式和学习类型推断。
- **LearningMAP**：其执行状态使用结构化 Schema，模型输出集中清理和校验，后台任务可查询状态，页面状态可由持久化数据重建，独立记忆服务不可用时主流程仍可降级。可借鉴这些服务边界；不迁移其 FastAPI 架构，也不在一键分析阶段复制整套图工作流。
- **OpenTutor**：其稳定、实验和路线图能力分级值得借鉴，AI/本地模型也不是全部基础功能的硬依赖。本站公开多用户部署不能采用它的单用户默认，且应额外保留认证、多用户隔离和上线前密钥泄漏检查。
- **LangChain 官方方案**：上下文应分成单次模型上下文、会话内状态和跨会话存储；长对话可采用“近期原文 + 持久化摘要”，模型重试、工具次数和上下文大小应有限制。第一版只实现所需部分，不因框架提供某项中间件就默认启用。

### 立即借鉴的设计

1. **指标 → 洞察 → 行动 → 结果**：数据库和规则先产生可复核指标，再形成风险洞察，AI 只解释并提出行动；系统记录用户是否采纳以及后续是否改善。
2. **题目到知识点的显式映射**：增加统一的技能/知识点词表和 `problem_skill_map`，让分析能回答“哪个知识点薄弱”，而不是只罗列题号。
3. **结构化学生模型**：将掌握证据、重复错误、近期会话摘要、已确认目标分开保存；掌握结论必须带样本数、置信度和数据时间。
4. **按任务裁剪并设置硬上限**：每类信息独立 Top-N，诊断任务优先低掌握、高风险和近期异常，不能固定按“最强项优先”，更不能塞入全部历史。
5. **集中模型边界**：所有模型请求经过一个适配层；Prompt 独立保存并有版本；模型 JSON 在一个位置修复和校验，最多进行一次受控修复。
6. **可恢复的后台任务**：分析任务和结果持久化，页面刷新后能恢复 queued/running/succeeded/failed 状态，并允许取消尚未完成的任务。
7. **确定性降级**：学生记忆、模型或外部服务不可用时，阶段 1 的规则分析仍能工作；可选 AI 不能成为原站启动依赖。

### 后续满足条件再采用

| 能力 | 采用前提 | 最早评估阶段 |
|---|---|---|
| Bayesian Knowledge Tracing（BKT） | 已有稳定知识点词表、题目映射、足够尝试数据，并完成离线校准 | 阶段 6 |
| LangGraph 持久状态图 | 普通请求/后台任务无法清晰处理跨请求暂停、恢复和用户确认 | 阶段 5 |
| 向量检索和长期语义记忆 | 结构化 SQLite 检索已不能满足规模或召回需求，并有真实评测证明收益 | 阶段 6 |
| 完整 FSRS 替换 | 现有间隔复习出现已证实的问题，且迁移不会破坏既有轮次语义 | 独立项目 |
| 多 Agent 协作 | 单链或单 Agent 无法完成明确任务，并能控制成本、权限和循环 | 阶段 6 以后 |

### 当前明确不采用

- 不因参考项目迁移现有 Python HTTP 服务、SQLite、多用户认证或前端技术栈；
- 不使用“视觉型/听觉型学习者”、人格、认知负荷等缺少可靠证据的自动标签；
- 不让模型直接从完整聊天记录或完整学习流水中自行找重点；
- 不把模型生成的 HTML、JavaScript、SQL、Shell 或第三方生成文件当作可执行内容；
- 不为有限课程库提前引入向量数据库、独立记忆微服务或知识图数据库；
- 不把第三方项目当作生产依赖直接嵌入，先用本项目接口复现必要模式并测试。

### 调研后对阶段顺序的修正

调研确认原六阶段顺序总体合理，但补充三个前置门槛：

1. 阶段 0 必须同时建立“指标词典”和“技能/知识点词表草案”；
2. 阶段 2 必须先完成按任务筛选、Top-N 硬限制和短/长期信息边界，再允许接模型；
3. 阶段 3 必须做到模型适配集中、任务可恢复、Prompt 可版本化和结果可校验，才开放入口。

因此施工仍从阶段 0 开始，不先搭 Agent，不先引入记忆框架，也不先进行技术栈改造。

---

## 4. 总体结构

```text
用户操作
  ↓
InterviewForge API
  ↓
学习分析器（确定性事实）
  ↓
上下文编译器（按任务裁剪、脱敏、预算）
  ↓
LangChain 模型层（结构化生成）
  ↓
结果校验器（证据、题号、课程 ID）
  ↓
页面展示或用户确认后的计划写入
```

建议模块边界：

```text
tools/
├─ study_server.py             # 现有服务与薄路由适配
├─ learning_analytics.py       # 确定性统计和风险信号
├─ ai_context.py               # 上下文模板、筛选、预算、脱敏
├─ ai_schemas.py               # Pydantic 输入输出结构
├─ ai_chain.py                 # LangChain 模型调用和分析链
├─ ai_coach.py                 # 任务、缓存、校验、降级
├─ course_metadata.*           # 后续课程元数据，具体格式阶段 4 决定
└─ tests/
   ├─ test_learning_analytics.py
   ├─ test_ai_context.py
   ├─ test_ai_chain.py
   └─ test_ai_coach.py
```

文件名可在施工时微调，但必须保持“数据计算、上下文、模型调用、业务编排”四层分离，避免所有功能继续堆进 `study_server.py`。

---

## 5. 阶段总览

计划压缩为六个施工阶段和一个持续维护阶段。

| 阶段 | 名称 | 核心成果 | LangChain | 数据库变化 | 默认部署策略 |
|---|---|---|---:|---:|---|
| 0 | 数据盘点与指标契约 | 数据字典、指标定义、合成样本 | 否 | 否 | 不部署 |
| 1 | 确定性学习分析 | 分析模块、只读分析 API | 否 | 否 | 可部署但不开放入口 |
| 2 | 上下文编译器 | 任务模板、裁剪、脱敏、快照 | 否 | 否 | 可部署但仅测试 |
| 3 | LangChain 一键学习分析 | 模型接入、结构化结果、中控台入口 | 是 | 缓存表可选 | 先灰度再开放 |
| 4 | 课程元数据与路线推荐 | 规则召回、AI 解释、学习顺序 | 是 | 通常不需要 | 先规则后 AI |
| 5 | 学习计划与对话式 Agent | 计划确认、多轮追问、受控工具 | 是，必要时 LangGraph | 是 | 独立灰度 |
| 6 | 持续质量与成本优化 | 质量、费用、反馈和模型升级 | 是 | 可选 | 持续进行 |

---

## 6. 阶段 0：数据盘点与指标契约

状态：`COMPLETE`（2026-09-07，交接见 `docs/ai-handoffs/phase-0.md`）

### 6.1 目标

明确哪些数据重要、每个指标怎样计算，并建立不含真实用户信息的测试样本。本阶段只写数据与测试设计，不实现业务功能。

### 6.2 工作范围

盘点当前数据：

- `study_events`：题目浏览与历史完成事件；
- `content_events`：书架章节浏览与完成轮次；
- `submissions`：AC/WA、语言、耗时、内存、时间和来源；
- `marks`：mastered、reviewing、weak；
- `plan_pins` 和现有今日计划；
- `/api/dashboard`、`/api/daily`、`/api/plan` 的已有聚合；
- `library_catalog.py` 中课程标题、分类、简介和顺序；
- 每用户独立 SQLite 与 `auth.db` 的边界。

定义总体指标：

- 已完成题目数和完成比例；
- 当前总体轮次；
- 最近 7/14/30 天活跃天数；
- 连续学习天数；
- 今日完成数；
- 到期和逾期数量；
- 模块和章节进度；
- 最近学习时间。

定义题目指标：

- 浏览次数；
- AC、WA 和总提交次数；
- 出现 AC 的自然日数量；
- 最后一次提交状态；
- 最后 AC、WA 和活动时间；
- 标记状态；
- 下次复习日期和逾期天数。

定义第一批风险信号：

- `repeat_wa`：多次 WA 且尚未 AC；
- `wa_after_ac`：曾经 AC，之后又出现 WA；
- `view_without_ac`：多次浏览但没有 AC；
- `due_overdue`：已到期但没有复习；
- `stalled_module`：课程已开始但长期没有推进；
- `data_insufficient`：数据不足，不应下强结论。

建立第一版技能/知识点模型草案：

- 盘点现有题目分类、书架目录和可复用标签；
- 定义统一 `skill_id`、名称、别名、父子关系和版本；
- 定义题目到一个或多个技能的映射格式；
- 本阶段允许映射不完整，但必须标记覆盖率和“未映射”，不能让 AI 猜测；
- 暂不计算 BKT，只保留以后扩展所需的尝试次数、最近证据和置信度字段。

统一分析术语：

```text
indicator（指标） → insight/signal（规则洞察） → action（建议行动） → outcome（采纳与后续结果）
```

每个定义都记录版本和计算假设，避免后续修改阈值却无法解释历史结果。

### 6.3 必须保留的现有语义

- Hot 100 每道题按不同自然日的 AC 数计算轮次；
- 同一天多次 AC 只增加一轮，但提交次数按实际记录；
- 书架章节继续按 `content_events.complete` 计算轮次；
- 浏览不能等同于掌握；
- 手动记录和力扣同步记录保留来源差异；
- “数据不足”必须成为正式结果，而不是异常。

### 6.4 合成测试场景

至少覆盖：

1. 新用户空数据；
2. 只有浏览没有提交；
3. 多次 WA 从未 AC；
4. WA 后 AC；
5. AC 后再次 WA；
6. 同日多次 AC；
7. 跨日多次 AC；
8. 薄弱标记与最近表现冲突；
9. 章节完成后逾期；
10. 大量旧数据但近期无活动；
11. 手动记录和同步记录混合；
12. 时区和跨自然日边界。

### 6.5 测试与审查

- 人工核对每个指标的输入、算法和边界；
- 合成样本必须写出预期结果；
- 不使用任何真实用户数据库；
- 集中审查一次，重点检查轮次、时间、数据来源和“数据不足”语义。

### 6.6 交付与完成条件

- 数据字典；
- 指标契约 v1；
- 风险信号阈值表；
- 技能/知识点词表草案、题目映射协议和覆盖率；
- 合成测试样本与预期结果；
- `docs/ai-handoffs/phase-0.md`。

全部通过后等待用户确认是否进入阶段 1。

---

## 7. 阶段 1：确定性学习分析

状态：`COMPLETE`（2026-09-07，交接见 `docs/ai-handoffs/phase-1.md`）

### 7.1 目标

不调用 AI，先让系统准确回答“用户学了什么、哪里出现异常、有哪些数据证据”。

### 7.2 工作范围

- 新建 `learning_analytics.py`；
- 复用现有统计语义，避免和 `dashboard_data()` 产生两套定义；
- 计算阶段 0 的指标和风险信号；
- 每条信号关联稳定的证据 ID；
- 生成结构化 JSON；
- 提供当前登录用户可访问的只读分析 API；
- 增加轻量缓存，并在相关学习记录变化后失效；
- 不修改现有 API 的响应结构。

建议响应：

```json
{
  "schema_version": "analytics-v1",
  "data_as_of": "2026-09-07T16:20:00+08:00",
  "summary": {},
  "problem_metrics": [],
  "module_metrics": [],
  "signals": [],
  "evidence": [],
  "data_quality": {}
}
```

其中 `problem_metrics` 应逐步带上已映射的 `skill_ids`、样本数和置信度；`signals` 是静态规则产生的可解释洞察，不包装成预测模型。每条建议行动都应能追溯到 signal 和 evidence，为后续记录 outcome 留出稳定 ID。

### 7.3 暂不包含

- LangChain；
- 模型 API；
- AI 页面入口；
- 新增 AI 数据表；
- 课程路线推荐。

### 7.4 测试

- 阶段 0 全部金标准样本；
- 空库和旧库；
- 多用户隔离；
- 缓存命中和失效；
- 10,000 条提交记录的性能；
- SQLite 并发读写；
- 原有 `/api/dashboard`、`/api/daily`、`/api/plan` 回归；
- `python .\tools\check_hot100.py` 必须 `errors: 0`。

### 7.5 集中审查重点

- SQL 和时间计算是否正确；
- 是否读取了当前用户之外的数据；
- 缓存是否按用户隔离；
- 每个风险信号是否有真实证据；
- 指标、洞察、行动之间能否稳定追溯；
- 是否重复实现并改变现有轮次语义；
- 新接口是否有无界返回。

### 7.6 交付与完成条件

- 分析模块和只读 API；
- 自动测试；
- 典型响应样例；
- 一次集中代码审查及修复记录；
- `docs/ai-handoffs/phase-1.md`。

指标测试必须全部通过，原有功能无回归，才可交接。

### 7.7 部署

只有用户明确要求部署时才部署。即使部署，也先不显示普通用户入口，只验证后端只读接口和原站稳定性。

---

## 8. 阶段 2：上下文编译器

状态：`COMPLETE`（2026-09-07，交接见 `docs/ai-handoffs/phase-2.md`）

### 8.1 目标

根据任务从分析结果中挑选最重要的信息，形成有限、相关、脱敏、可复现的模型上下文。本阶段仍不安装或调用 LangChain。

### 8.2 第一批上下文任务

```text
learning_diagnosis  一键学习情况分析
today_plan          今日学习建议
problem_review      单题复盘
learning_route      仅定义预留协议，阶段 4 才实现课程召回
```

### 8.3 上下文结构

```json
{
  "context_schema_version": "context-v1",
  "task": "learning_diagnosis",
  "user_request": "...",
  "profile": {},
  "summary": {},
  "facts": [],
  "signals": [],
  "evidence": [],
  "data_quality": {},
  "selection_reasons": [],
  "omitted": {},
  "data_as_of": "...",
  "snapshot_hash": "..."
}
```

事实、规则推断、证据和未来 AI 建议必须分开。

### 8.4 信息筛选

排序参考：

```text
优先级 = 与当前任务的相关性 × 新鲜度 × 异常程度 × 可行动性
```

默认限制：

- 风险题目最多 10 道；
- 逾期题目最多 10 道；
- 薄弱模块最多 5 个；
- 最近活动最多 14 天日汇总；
- 单题最近提交最多 5 条；
- 课程候选最多 8 本；
- 默认不附完整题面和书籍正文。

每个入选项目保留选择原因；被省略的低优先级信息只记录数量。

Top-N 必须按任务分别排序：学习诊断优先低掌握、高频错误、近期退步和可行动问题；学习鼓励可以补充少量稳定优势；单题复盘优先该题近期证据。不得用一套固定排序服务所有任务。

短期与长期信息分开：

- 短期状态：当前请求、最近活动、当前分析任务；
- 长期事实：用户明确确认的目标、可用时间和稳定偏好；
- 学习事实：每次从数据库重新计算，不从旧对话记忆继承；
- 会话历史：后续只保留近期原文和结构化摘要，不把完整消息历史长期塞入 Prompt。

### 8.5 上下文预算

第一版采用字符上限、条目上限和粗略 token 估算，不额外引入 tokenizer。

| 档位 | 适用任务 | 输入目标 |
|---|---|---:|
| small | 今日建议、简短追问 | 2K～4K tokens |
| medium | 一键学习分析 | 4K～8K tokens |
| large | 路线规划、单题深度复盘 | 8K～12K tokens |

超过预算时按优先级裁剪，不能截断 JSON 或拆散证据关系。

### 8.6 数据白名单

默认只允许学习相关字段。以下内容永不进入上下文：

- 密码、会话、力扣 Cookie、CSRF 和 API Key；
- 头像二进制、联系方式、User-Agent；
- 管理员信息；
- 聊天室内容；
- 其他用户数据；
- 与任务无关的用户名和昵称。

必须按白名单构建上下文，不能“导出全部后再删除敏感字段”。

### 8.7 注入防护

- 用户输入、题面、题解和书籍正文都标记为不可信资料；
- 资料中的命令不能改变系统规则；
- 不执行模型或内容提供的 HTML、JavaScript、SQL 和 Shell；
- 第一版不允许上下文触发任何写操作。

### 8.8 快照与缓存键

上下文包含数据版本、指标版本和 `snapshot_hash`。相同学习事实应生成相同哈希；每次变化的 `generated_at` 不参与哈希。

后续模型缓存键使用：

```text
task + snapshot_hash + prompt_version + model
```

### 8.9 测试

- 各任务字段白名单；
- 上下文长度和裁剪顺序；
- 相同数据哈希一致；
- 数据变化后哈希变化；
- 敏感字符串哨兵；
- 提示词注入样本；
- 空、少量和超大数据；
- 中文、特殊字符和超长输入；
- JSON 序列化和证据引用完整性。
- 不同任务对同一批数据的排序和选取不同；
- 任何类别都不能通过数量膨胀绕过整体预算。

### 8.10 集中审查重点

- 是否按任务取数；
- 是否有敏感字段或跨用户数据；
- 裁剪后证据是否完整；
- 预算是否真正有硬上限；
- 快照是否可复现；
- 是否把聊天室或完整书籍意外放入上下文。

### 8.11 交付与完成条件

- 上下文编译模块；
- 三种可用任务模板和一份路线预留协议；
- 自动测试；
- 若干上下文预览样例；
- 一次集中审查及修复记录；
- `docs/ai-handoffs/phase-2.md`。

所有上下文必须能在不调用模型时独立检查。完成后等待用户确认是否接入 LangChain。

---

## 9. 阶段 3：LangChain 一键学习分析

状态：`COMPLETE`（2026-09-07，交接见 `docs/ai-handoffs/phase-3.md`；未部署）

### 9.0 实际交付摘要

- 新增可选 LangChain ChatModel 适配层和 Pydantic 结构化结果校验；不创建自主 Agent；
- 接通 `analytics-v1 → context-v1 → 单一模型入口 → 本地证据校验 → 持久化后台任务 → 中控台卡片`；
- 每用户学习库新增幂等 `ai_tasks`、`ai_insights` 表，支持恢复、取消、反馈、快照复用、全局并发上限和每日额度；
- 新增 `requirements-ai.txt`，AI 依赖延迟加载；默认 `AI_ENABLED=0`，未安装依赖时原站仍可用；
- 完成 16 项阶段 3 测试和 77 项全量回归；未执行真实模型调用，等待安全环境配置；
- 未部署、未提交 Git、未修改课程路线和 Agent 能力。

### 9.1 目标

使用 LangChain 接入第一个真实 AI 功能，在中控台提供“一键学习分析”。

### 9.2 LangChain 使用方式

本阶段不创建自主 Agent，使用最小链路：

```text
上下文 JSON
  → LangChain ChatModel
  → Pydantic 结构化输出
  → 本地结果校验
  → 页面卡片
```

优先使用模型原生结构化输出；供应商不支持或不稳定时使用 LangChain 的工具式结构化策略。无论使用哪种方式，后端都要再次校验。

所有模型调用必须经过单一客户端/适配入口；Prompt 使用独立模板并记录 `prompt_version`，不散落在路由和页面代码中。输出清理、一次受控修复、Pydantic 校验和证据校验也只保留一条统一链路。

建议输出模型：

```json
{
  "summary": "...",
  "strengths": [],
  "weaknesses": [
    {
      "title": "...",
      "explanation": "...",
      "evidence_ids": []
    }
  ],
  "actions": [],
  "confidence": "medium",
  "data_gaps": []
}
```

### 9.3 模型配置

建议配置：

```text
AI_ENABLED=0
AI_PROVIDER=
AI_MODEL=
AI_BASE_URL=
AI_API_KEY=
AI_REQUEST_TIMEOUT_SECONDS=45
AI_MAX_CONCURRENT_REQUESTS=2
AI_DAILY_LIMIT_PER_USER=10
AI_BETA_USERS=
```

要求：

- 默认关闭；
- 密钥只保存在服务器环境，不写入 Git；
- 缺少 LangChain 或配置时原站照常启动；
- 不记录完整 Prompt、凭证和模型密钥；
- 供应商可以替换，不让前端感知具体 SDK。

### 9.4 任务与缓存

模型响应较慢，使用后台任务，不长期占用页面请求：

```text
POST /api/coach/analyze       → task_id
GET  /api/coach/tasks/<id>    → queued/running/succeeded/failed
POST /api/coach/tasks/<id>/cancel → 取消尚未完成的任务
```

要求：

- 任务绑定当前用户；
- 有全局并发上限和单用户每日额度；
- 有超时，不无限重试；
- 相同快照优先复用缓存；
- 页面刷新不会重复创建任务；
- 页面刷新后能从持久化状态恢复当前任务和最近结果；
- AI 失败时返回阶段 1 的规则分析。

是否新增 `ai_tasks`、`ai_insights` 表，在本阶段施工前设计并一次完成。AI 表放在每个用户自己的学习库，不放入 `auth.db`。

### 9.5 结果校验

- Pydantic 类型和长度校验；
- 题号、课程 ID 和证据 ID 必须真实存在；
- 每个重要问题必须有证据；
- 不存在的证据引用直接拒绝；
- 输出作为普通文本转义，不渲染模型提供的 HTML；
- 解析失败最多修复重试一次；
- 仍失败则展示规则摘要。

### 9.6 中控台体验

页面提供：

- 上次分析摘要和数据时间；
- “开始分析/重新分析”按钮；
- 分析中、完成、失败状态；
- 优势、问题、依据和行动建议；
- 数据完整度和可信度；
- “查看本次使用了哪些数据”；
- “有帮助/没帮助”反馈。

反馈不仅统计按钮次数，还应关联本次 insight/action；后续阶段可判断建议是否被采纳，以及相关指标是否改善。第一版不声称这种关联等于因果效果。

不在用户每次打开页面时自动调用模型，避免无意义消耗。

### 9.7 降级

AI 未配置、超时、429、服务端错误、格式错误、额度耗尽或并发已满时：

- 原站正常；
- 规则分析仍可查看；
- 用户看到简洁原因和稍后重试提示；
- 不显示供应商内部错误和敏感日志。

### 9.8 测试

- 使用假模型测试正常、超时、429、500、非法 JSON、超长输出；
- Pydantic 校验；
- 错误证据 ID；
- 缓存、并发和频控；
- 多用户任务隔离；
- AI 未安装和关闭状态；
- 数据库迁移幂等；
- 页面桌面/移动、深色/浅色；
- XSS 和重复点击；
- 页面刷新后的任务恢复和任务取消；
- Prompt 版本、集中适配入口和单次修复上限；
- 上线前执行一次密钥泄漏检查，确保配置和历史待提交内容不含凭证；
- 原站全部回归检查。

真实模型只做少量端到端验证，不用真实调用代替自动测试。

### 9.9 集中审查重点

- API Key 和上下文是否泄漏；
- 模型故障是否拖垮当前 HTTP 服务；
- 任务和缓存是否跨用户；
- 结果校验和规则降级是否可靠；
- 页面是否清楚区分事实与 AI 建议；
- 调用次数和成本是否有上限。

### 9.10 交付与完成条件

- `requirements-ai.txt` 和锁定版本说明；
- LangChain 模型层、结构化 Schema 和任务接口；
- 一键分析页面；
- 自动测试与真实模型小规模验证；
- 一次集中审查及修复记录；
- `docs/ai-handoffs/phase-3.md`。

### 9.11 部署

只有用户明确要求部署时进行：

1. 备份数据；
2. 部署时保持 `AI_ENABLED=0`；
3. 验证原站；
4. 只向管理员或 beta 用户开启；
5. 观察错误、延迟、费用和服务器状态；
6. 修复后再决定是否开放普通用户。

### 9.12 阶段 3.1：上下文信息密度优化

状态：`COMPLETE`（2026-09-08，交接追加于 `docs/ai-handoffs/phase-3.md`；未部署）

- 新增 `diagnostic_digest` 确定性高密度摘要，覆盖概览、复习积压、逾期分桶、轮次分布、代表案例、异常观察和 source/预算覆盖区分；
- 诊断信号按类型多样性采样，逾期类不再垄断；只有逾期类时最多保留 3 个代表信号，其余由摘要统计承载；
- 诊断模型投影压缩重复 facts/signals 元数据，移除 submission source distribution，保留 evidence 闭包和 digest；实际快照模型投影约 7,711 字符，约 1.9K 粗略 token；
- action 新增严格 `basis=data|heuristic` 和 `confidence`，页面展示“数据依据/通用建议”，旧 insight 兼容；
- `model_prepared` 追加最终投影尺寸、token 估算、sections 计数、Prompt/context 哈希、非敏感 model key 和未发送区段；日志只追加，不清空；
- 相关上下文测试 15 项、AI 测试 21 项；合并 analytics/analytics API/context/AI 测试 86 项全部通过；编译检查通过；无已知 P0/P1；
- 本阶段没有创建子代理、没有启动阶段 4、没有部署，也没有改动密钥或 `.maintenance/ai-debug.jsonl`。

---

## 10. 阶段 4：课程元数据与学习路线推荐

状态：`NOT_STARTED`

### 10.1 目标

支持用户输入“我想学 Java 高并发”“我准备 Agent 岗位”“我只有两周”，系统推荐真实存在的书籍和学习顺序。

### 10.2 先做课程元数据

建议补充：

```json
{
  "id": "java-concurrency",
  "tags": ["Java", "并发", "线程池", "JMM", "锁"],
  "aliases": ["高并发", "多线程"],
  "prerequisites": ["java-core"],
  "difficulty": 3,
  "estimated_hours": 12,
  "career_tracks": ["Java后端", "校招"],
  "outcomes": ["能解释 JMM", "能分析线程池参数"]
}
```

施工时在扩展 `library_catalog.py` 和独立元数据文件之间二选一，不能形成两个权威来源。

元数据必须校验：

- 模块 ID 真实存在；
- 前置课程不存在自指和环；
- 难度和时间范围有效；
- 标签、别名和学习结果经过人工核对。

课程技能标签与阶段 0 的统一技能词表使用同一 `skill_id`。题目、书架课程和学习路线由技能层连接，避免分别维护三套同义标签。

### 10.3 规则召回先行

不用 AI 也应能完成：

```text
目标关键词和别名匹配
  → 找到候选课程
  → 补齐前置课程
  → 结合技能缺口、已学进度和薄弱点排序
  → 按依赖图排列
  → 根据可用时间裁剪
```

第一版不需要向量数据库。已有课程数量有限，结构化元数据、关键词和现有全文搜索已经足够，也更容易解释和测试。

### 10.4 LangChain 的职责

规则引擎给出最多 8 本候选课程后，LangChain 负责：

- 理解用户自然语言目标；
- 对候选课程排序；
- 解释为什么推荐；
- 根据已有基础减少重复学习；
- 根据时间限制生成阶段安排；
- 在信息不足时提出最多两个必要问题。

AI 不能自行创造候选集外的课程。最终结果由后端再次校验课程 ID 和前置顺序。

### 10.5 可用功能

- 系统学习、面试冲刺、查漏补缺三种模式；
- 7 天、14 天、30 天路线；
- “我已经会了”手动排除；
- “为什么推荐”依据；
- 前置关系和预计时间展示；
- AI 不可用时仍提供纯规则路线。

### 10.6 测试

- Java 后端、AI/Agent、RAG 等典型路线；
- 零基础和已有基础；
- 只有 7 天或每天 30 分钟；
- 同义词、中英文和模糊目标；
- 无匹配目标；
- 前置课程补齐与环检测；
- 已完成课程不被无理由重复推荐；
- AI 返回不存在课程或错误顺序；
- 规则模式和 AI 模式结果都可用。

### 10.7 集中审查重点

- 课程关系是否符合真实内容；
- 推荐是否来自真实课程；
- 顺序是否满足前置关系；
- 时间估算是否明确为估计；
- 用户学习进度是否被正确使用；
- 没有 AI 时是否仍可完成基本推荐。

### 10.8 交付与完成条件

- 课程元数据和校验；
- 纯规则召回与排序；
- LangChain 路线解释；
- 页面路线展示；
- 自动测试和一次集中审查；
- `docs/ai-handoffs/phase-4.md`。

只有规则路线先通过人工核对，才接入 AI 解释。

---

## 11. 阶段 5：学习计划与对话式 Agent

状态：`NOT_STARTED`

### 11.1 目标

让用户围绕分析和路线继续追问，并可在确认后保存学习计划。

### 11.2 LangChain Agent 工具

第一批只读工具：

```text
get_learning_summary()
get_problem_stats(problem_ids)
get_due_items(limit)
get_recent_submissions(problem_id, limit)
search_modules(query, limit)
get_module_outline(module_id)
get_active_plan()
```

要求：

- 工具由服务端绑定当前用户，不接收任意用户 ID；
- 参数和返回数量有限制；
- 返回值继续经过上下文预算；
- 每轮工具调用次数有上限；
- 不存在通用 SQL、文件、Shell、管理后台或聊天室工具。

### 11.3 写计划

第一版只考虑一个写工具：

```text
save_learning_plan(validated_plan)
```

但模型不能直接执行。流程固定为：

```text
AI 生成计划草案
  → 页面展示完整变更
  → 用户确认或修改
  → 后端校验题目、课程、日期和工作量
  → 保存计划
```

若普通应用层确认足够，就不引入 LangGraph；只有需要跨请求暂停、恢复和复杂工具流程时才使用 LangGraph 检查点与 human-in-the-loop。

### 11.4 对话记忆

- 最近 4～8 轮保留原文；
- 更早内容压缩成结构化摘要；
- 学习事实每轮重新从数据库读取；
- 长期目标和可用时间只有用户明确确认后才保存；
- 聊天室公屏不进入个人记忆；
- 用户可以清除自己的 AI 会话和偏好。

长期记忆只保存结构化、可查看的数据：已确认目标、时间约束、用户纠正和已解决/未解决的重复错误。每条记忆带来源、更新时间和状态；AI 推断不能在未经确认时升级为永久用户事实。

### 11.5 数据表

按实际需要增加：

```text
learning_plans
ai_sessions
ai_messages
ai_memories
```

新增表与原有学习记录分离。代码回滚时不删除这些表，AI 关闭后不影响原站。

### 11.6 测试

- 多轮追问和历史裁剪；
- 数据库新事实覆盖旧对话事实；
- 工具参数注入和越权用户 ID；
- 工具调用循环上限；
- 未授权工具；
- 用户拒绝、修改和确认计划；
- 重复确认幂等；
- 计划写入失败和回滚；
- 会话清除；
- Agent 或 LangGraph 不可用时降级。

### 11.7 集中审查重点

- 工具是否严格白名单；
- 是否可能绕过用户确认；
- 是否存在无限工具循环；
- 对话摘要是否把 AI 推断保存成长期事实；
- 用户能否删除 AI 数据；
- 写入计划是否幂等并保持旧计划可恢复。

### 11.8 交付与完成条件

- 对话式学习教练；
- 白名单只读工具；
- 计划草案和确认写入；
- 对话和偏好清除；
- 自动测试和一次集中审查；
- `docs/ai-handoffs/phase-5.md`。

写操作测试和确认流程未通过前，不向普通用户开放。

---

## 12. 阶段 6：持续质量与成本优化

状态：`NOT_STARTED`

这是上线后的持续工作，不设置复杂审计制度，只维护必要指标和定期回归。

### 12.1 需要观察的指标

- 调用成功率和降级率；
- 结构化输出解析成功率；
- 证据引用有效率；
- 平均与 P95 响应时间；
- 每用户调用量；
- 输入/输出 token 和费用；
- 缓存命中率；
- “有帮助/没帮助”比例；
- 推荐路线采用率；
- insight 对应 action 的采纳率、完成率和后续指标变化；
- 不存在题目或课程的生成次数。

### 12.2 定期工作

- 模型或 Prompt 升级前跑固定评测样本；
- 每月检查一次密钥、日志和跨用户缓存；
- 根据费用和延迟调整缓存、预算和限额；
- 根据用户反馈修正指标阈值和课程元数据；
- 数据量和知识点映射质量达到门槛后，再离线评估 BKT 是否优于现有确定性评分；
- 保留 `AI_ENABLED=0` 的快速降级能力；
- 不保存不必要的完整 Prompt 和用户内容。

### 12.3 后续可选功能

- 周报和月报；
- 与上周相比的趋势分析；
- 面试日期倒排计划；
- 模拟面试薄弱点回流；
- 用户纠正 AI 结论；
- “每天多学 30 分钟会怎样”的路线模拟；
- 匿名聚合的系统用量与故障面板；
- 本地模型作为隐私优先备选。

这些功能各自作为小迭代施工，不自动纳入前五阶段。

---

## 13. 通用测试要求

每个代码阶段都必须：

- 使用临时 SQLite 和合成数据，不读取真实用户库；
- 为新增核心逻辑写自动测试；
- 测试空数据、边界值和失败路径；
- 测试多用户隔离；
- 保证现有登录、题解、书架、中控台、聊天和管理后台不回归；
- 运行 `python .\tools\check_hot100.py` 并保持 `errors: 0`；
- 如果修改生成器，重新构建相应产物后再检查；
- 如果增加数据库表，验证新库、旧库和重复迁移；
- 如果接入模型，使用假模型覆盖主要异常，再少量调用真实模型。

测试失败时不进入交接。

---

## 14. 安全与隐私底线

只保留必须遵守的底线，不在每阶段重复写长审计清单：

1. 只读取当前登录用户自己的学习库；
2. `auth.db` 不作为学习上下文来源；
3. 密码、会话、力扣凭证和 API Key 永不进入模型上下文；
4. 聊天室内容默认永不进入个人学习上下文；
5. 模型输出按不可信文本处理并转义；
6. 写计划必须由用户明确确认；
7. 不向 Agent 暴露通用 SQL、文件、Shell 和管理员工具；
8. AI 关闭或故障时原站完整可用；
9. 测试不使用真实用户数据；
10. 用户可以查看数据范围并清除自己的 AI 会话和计划。

---

## 15. 数据库与部署规则

### 15.1 数据库

- 新表使用幂等迁移；
- AI 表与现有学习记录表分离；
- 迁移先在数据库副本测试；
- 上线前备份 `data/`；
- 代码回滚不删除新增表；
- 优先通过 `AI_ENABLED=0` 快速关闭 AI。

### 15.2 部署

用户未明确要求施工时，不改代码；用户未要求部署时，不因计划更新自动部署。

涉及 AI 的阶段部署遵循简化流程：

1. 本地测试和集中审查通过；
2. 若有数据库变化，先备份；
3. 部署时默认关闭 AI；
4. 验证 `/api/health` 和现有主要页面；
5. 只对测试账号开启；
6. 检查延迟、错误、费用和日志；
7. 再决定是否开放普通用户。

不修改共享 VPS 上与 InterviewForge 无关的服务和配置。

---

## 16. 简化交接模板

每个阶段结束只需填写一份：

```markdown
# AI 学习教练阶段 X 交接

## 完成内容
-

## 修改文件
-

## 数据库与配置
- 无 / 具体变化

## 测试结果
- 自动测试：
- 全站检查：
- 手工验证：

## 集中审查
- 发现的问题：
- 已修复：
- 保留限制：

## 部署与回滚
- 是否部署：
- 生产验证：
- 回滚点：

## 下一阶段交接说明
- 可以继续的部分：
- 不应提前做的部分：
```

---

## 17. 当前下一步

阶段 0 已完成：

1. 数据字典；
2. 指标与风险信号契约；
3. 技能/知识点词表草案与题目映射协议；
4. 14 个合成测试场景；
5. 一次集中审阅；
6. `docs/ai-handoffs/phase-0.md` 交接报告。

阶段 0 没有安装 LangChain、接入模型、修改前端或数据库，也没有部署。

阶段 2 已完成并交接：已实现不接入 LangChain/模型的确定性上下文选择、脱敏、硬预算、证据闭包和稳定快照哈希，并保留隐藏只读编译接口。

阶段 3 已完成并交接：已实现可选 LangChain 一键学习分析、后台任务、结果校验、规则降级和中控台卡片；未部署。

阶段 3.1 已完成并交接：已实现诊断摘要、逾期多样性采样、模型投影压缩、行动依据契约和追加式可观测性；86 项合并测试通过，未部署。当前等待用户确认是否进入“阶段 4：课程元数据与学习路线推荐”。

阶段 3.2 已完成实施：正式建立 `FullContext → DiagnosticDigest → LLMContext v2` 三层边界；完整追溯数据仅保留在后端，模型使用短 ref 与动态 compact facts，普通 API/UI 只展示自然语言案例标签。继续保留 DeepSeek 普通 JSON 与 ChatGPT structured output 两条路径，不部署、不启动阶段 4。最终测试结果以 `docs/ai-handoffs/phase-3.md` 的 3.2 交接为准。

阶段 3.3 已完成实施与一次真实基线：DeepSeek 服务端流式消费用于拆分 TTFT/生成耗时，用户端仍一次展示完整结果。真实请求 attempt=1、未 repair；模型总耗时约 21.34s，其中 TTFT 约 14.15s、生成约 7.19s，瓶颈更接近首 token 前。当前只保留观测结论，未关闭 thinking、未缩短输出、未切模型、未做 UI streaming，也未部署。后续优化候选见 `docs/ai-handoffs/phase-3.md` 第 13 节。

阶段 3.4 已完成：模型耗时进一步拆分为网络首 chunk、首正文、可见 reasoning、正文生成、解析和 validation；旧 TTFT/TPS 字段保留但增加定义，新增 content/reasoning token 语义与脱敏 request config hash。一次真实诊断和一次合成模拟诊断均只调用一次、无 repair，配置 hash 一致。两次网络首包均低于 0.2 秒，首正文约 71–73 秒、正文生成约 6–7 秒；当前 LangChain 版本丢弃 DeepSeek reasoning_content，因此精确 reasoning_ms/TPS 为 null，只能结合约 8.9K reasoning tokens 判断瓶颈高度接近首正文前 reasoning 阶段。未部署、未调整模型参数、未启动后续优化。

阶段 3.5 已完成：冻结同一份 2399 chars 的 LLMContext v2，按 ABABABABAB 做了 10 次真实 DeepSeek 首答基准，禁用 repair。A 保持当前 `deepseek-v4-flash + thinking enabled + high`，B 同模型仅将官方支持的 `thinking.type` 改为 disabled；默认配置前后 hash 一致。B 将 model total 中位数从约 79.4 秒降至 9.78 秒，但严格 validation 从 A 的 60% 降至 B 的 0%，主要问题为 schema/null、边界和非法 support refs，而非 JSON parse。当前不调整线上默认。若进入下一步，应先强化非 reasoning 输出契约及安全归一化，再以固定上下文复测；不得直接因速度结果切换默认模型。

独立额度/UI 小阶段已完成：cockpit 增加前端模拟百分比与五阶段提示，最短展示 2.8 秒，结果返回前上限 97%，成功才收尾到 100%，并兼容 reduced-motion 与 aria-live；普通用户每日额度固定为上海自然日 3 次，并使用“入队预留、首次外部请求前消费”的 SQLite 原子账本。缓存和本地前置失败不计费，已发起外部调用后的供应商或输出失败计费；API 返回结构化额度并以 429 拒绝超限。管理后台可查看并重置普通用户今日额度，auth.db 只保留最小重置审计。模型策略和阶段 3.5 benchmark 未改，尚未部署。

部署前题页小改动已完成：每题自身的统一力扣 URL 现在复用于“题目与约束”后和原有文末入口，HTML 外链安全属性保持不变；100 道题的 Markdown/HTML 已刷新。cockpit 继续使用既有版本化公告机制，用户公告只说明动态进度与处理阶段提示、每日 3 次、可联系管理员重置和双位置原题入口。尚未部署。

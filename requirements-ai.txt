# Optional AI capability for InterviewForge.  The base site does not install
# or import these packages when AI_ENABLED=0.
# Validated together on Python 3.11; keep versions pinned for reproducible VPS
# rollouts and upgrade only after the stage-3 fake-model tests pass.
langchain==0.3.27
langchain-core==0.3.72
langchain-openai==0.3.28
pydantic==2.11.7
# Windows does not ship the IANA zoneinfo database used by Asia/Shanghai.
tzdata==2025.2

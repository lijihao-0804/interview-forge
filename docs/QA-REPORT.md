# 整理与校验报告

[返回总目录](../README.md)

## 内容覆盖

- 原始 Markdown：22 篇。
- 原始不同题号：99 个。
- 原始解法段：107 个；重复版本已并列保留。
- 新增题目：226「翻转二叉树」。
- 整理后题目页：100 个，题号无重复，每页含“题目与约束”与力扣原题链接（100/100 已联网核验，新标签页打开）。
- 专题页：17 个。
- 可视化页面：15 个，其中 14 个交互演示和 1 个集中导航页；全部统一标题、面板、标签页、按钮、表单、消息区、图例和深色模式样式。
- 学习书架：37 个模块，709 个学习章节（含 Hot 100 的 100 题）。

## 自动校验结果

- Markdown 代码围栏：全部成对。
- 题页必需结构：全部具备“题目与约束”、“核心不变量”、复杂度和相邻学习入口，且不残留“先记住”。
- 完整 HTML：455 个；其中正文阅读页 424 个、可视化页 15 个、学习面板 1 个。
- 本地 Markdown/HTML 链接：0 个失效；日常 HTML 入口不再跳转到 Markdown。
- HTML 结构：全部具备 `lang`、`title` 与响应式 `viewport`，无重复 `id`。
- 数学内容：1222 处 `$...$` / `$$...$$` 已转换为本地浏览器原生 MathML；无残留 `$O(N)$`、`$\rightarrow$` 或 `&#x20;`。
- 维护能力：已提供 HTML 维护指南、可复制的新增题目模板和独立扩展题源目录；新增题目可进入全量生成流程。
- JavaScript：学习面板、公共脚本和 15 个可视化页全部通过 `node --check`。
- 浏览器运行测试：搜索“二叉树”得到 11 题，空结果提示正确；旧演示标签支持键盘 Enter，新增链表演示可正常步进。
- 响应式检查：在 1280、768、360、320 px 下抽查首页、长题解、超长 Java 速查、旧复杂演示、新演示、0-1 背包和长公式页面；无页面级横向溢出、控件越界或隐藏式裁切。
- 动画防遮挡：柱状图为上下标签预留安全区，链表指针标签与节点分离，水位浮层位于柱子下层，Canvas 使用独立安全容器，缩放高亮不再覆盖相邻单元格。

## 信息保全

- 源目录未修改。
- `99-原稿归档/` 保存原始 Markdown 副本。
- 源笔记中两张已失效的绝对路径图片已由题页内的“交互演示”提示替代，原始引用仍可在归档中核对。

## 可重复检查

```powershell
python .\tools\check_hot100.py
```

重新从源目录生成主知识库：

```powershell
python .\tools\build_hot100.py
```
